import React from 'react';
import VehicleList from './vehicleList';

const Gallery = () => {
return (
	<div>
		{/* <VehicleList searchResults={searchResults} /> */}
	</div>
);
};

export default Gallery;
